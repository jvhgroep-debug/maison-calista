{
  "name": "maison-calista",
  "private": true,
  "version": "1.0.0",
  "description": "Maison Calista — standalone static website (Cloudflare Workers Static Assets)",
  "scripts": {
    "build": "node _preview/build-static.js",
    "preview": "node _preview/server.js",
    "check:sitemap": "node _preview/validate-sitemap.js",
    "check": "npm run build && npm run check:sitemap && npx wrangler deploy --dry-run --outdir=.wrangler-dry-run",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
